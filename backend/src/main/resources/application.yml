spring:
  application:
    name: smartfridge-backend
  profiles:
    active: dev

app:
  cors:
    allowed-origin-patterns: https://smartfridge-backend-c27p.onrender.com

jwt:
  secret: ${JWT_SECRET:dev-only-secret-change-before-prod-32chars}
  expiration-ms: 86400000
